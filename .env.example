# Rolodex Contact Management - Environment Variables

# Express Port
PORT=5000

# Node Environment (development | production | test)
NODE_ENV=development

# MongoDB Connection String (leave as default or provide MongoDB Atlas URI)
# If local MongoDB is not running, the server automatically spins up an in-memory database!
MONGODB_URI=mongodb://127.0.0.1:27017/rolodex_contacts
